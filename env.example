# ========================================
# HSK学习平台 - 环境变量配置
# 复制此文件为 .env 并修改配置
# ========================================

# 数据库配置
# 注意：这里的 DB_NAME 需要和数据库实际的 schema 名一致。
# 目前你的备份 SQL 使用的是 hsk_schema 前缀，因此这里也改成 hsk_schema。
DB_NAME=hsk_schema
DB_USER=root
DB_PASSWORD=your_strong_password_here_change_me

# Django配置
SECRET_KEY=your_django_secret_key_here_change_me
# 注意：这里已经包含 .online 域名和服务器公网 IP（请根据实际 IP 修改）
ALLOWED_HOSTS=tangxinhao.online,www.tangxinhao.online,118.190.106.159,localhost

# API地址 (用于前端构建，实际前端使用 /api 相对路径，由 Nginx 反向代理)
API_BASE_URL=https://tangxinhao.online

# 域名配置
DOMAIN=tangxinhao.online
