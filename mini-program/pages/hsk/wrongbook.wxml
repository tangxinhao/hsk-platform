<view class="container">
  <view class="title">错题本</view>
  <van-list>
    <block wx:for="{{wrongs}}" wx:key="id">
      <van-cell title="{{item.question}}" label="{{item.answer}}" />
    </block>
  </van-list>
</view> 