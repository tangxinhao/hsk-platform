# 证书上传步骤（项目在 /home/hsk）

## 📍 你的项目位置

根据你的 FinalShell 截图，项目在：**`/home/hsk`**

所以证书目录应该是：**`/home/hsk/ssl/tangxinhao.online/`**

---

## 步骤 1：在项目目录下创建证书目录

在 FinalShell 终端中执行：

```bash
cd /home/hsk
mkdir -p ssl/tangxinhao.online
```

或者直接：
```bash
mkdir -p /home/hsk/ssl/tangxinhao.online
```

---

## 步骤 2：上传证书文件

### 方法一：使用 FinalShell 文件管理器（最简单）

1. **在 FinalShell 左侧文件管理器中**：
   - 导航到 `/home/hsk/ssl/tangxinhao.online/` 目录
   - 如果看不到，在左侧目录树中展开：
     - `/home` → `hsk` → 新建 `ssl` 文件夹（如果不存在）
     - 进入 `ssl` → 新建 `tangxinhao.online` 文件夹（如果不存在）
     - 进入 `tangxinhao.online` 文件夹

2. **在 FinalShell 文件管理器中打开本地文件**：
   - 在 FinalShell 文件管理器的**顶部**，应该有一个切换按钮可以切换到**本地文件系统**
   - 或者，直接打开你电脑的**文件资源管理器**（Windows 资源管理器）

3. **拖拽上传**：
   - 从本地电脑的"下载"文件夹中找到两个证书文件（`.pem` 和 `.key`）
   - **直接拖拽**这两个文件到 FinalShell 文件管理器的 `/home/hsk/ssl/tangxinhao.online/` 目录

4. **重命名文件**：
   - 在 FinalShell 文件管理器中，右键点击 `.pem` 文件 → **重命名** → 改为 `cert.pem`
   - 右键点击 `.key` 文件 → **重命名** → 改为 `cert.key`

### 方法二：使用 FinalShell 的上传按钮

1. 在 FinalShell 文件管理器中，进入 `/home/hsk/ssl/tangxinhao.online/`
2. 点击文件管理器工具栏的 **上传** 按钮（或右键空白处 → **上传**）
3. 选择本地电脑的两个证书文件
4. 上传后重命名为 `cert.pem` 和 `cert.key`

### 方法三：使用命令行（如果你已经下载了证书）

如果你已经在服务器上某个地方有证书文件，可以使用 `cp` 或 `mv` 命令：

```bash
# 假设证书文件在 /home/hsk/ 目录下
cd /home/hsk
mv 证书文件名.pem ssl/tangxinhao.online/cert.pem
mv 证书文件名.key ssl/tangxinhao.online/cert.key
```

---

## 步骤 3：验证文件已上传

在 FinalShell 终端中执行：

```bash
ls -la /home/hsk/ssl/tangxinhao.online/
```

应该看到：
```
cert.pem
cert.key
```

---

## 步骤 4：重启 Nginx

```bash
cd /home/hsk
docker restart hsk-nginx
```

---

## 步骤 5：测试 HTTPS

在浏览器访问：
- `https://www.tangxinhao.online`

如果看到锁图标 ✅，说明配置成功！

---

## 📝 重要提示

1. **证书目录位置**：`/home/hsk/ssl/tangxinhao.online/`
2. **文件名称**：必须是 `cert.pem` 和 `cert.key`
3. **如果证书文件已经在服务器上**：可以直接用 `mv` 或 `cp` 命令移动到正确位置
